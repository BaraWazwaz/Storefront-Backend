{
  "name": "storefront_backend",
  "version": "0.1.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "db:init": "tsx src/database/init.ts",
    "server:dev": "cross-env MODE=deploy tsx watch src/server.ts",
    "server:run": "cross-env MODE=deploy tsx src/server.ts",
    "migrate:create": "db-migrate create --sql-file -e deploy",
    "migrate:up": "db-migrate up -r tsx --no-compilation -e deploy",
    "migrate:down": "db-migrate down -r tsx --no-compilation -e deploy",
    "migrate:test:up": "db-migrate up -r tsx --no-compilation -e test",
    "migrate:test:down": "db-migrate reset -r tsx --no-compilation -e test",
    "test": "yarn migrate:test:up && cross-env MODE=test tsx src/helpers/reporter.ts && yarn migrate:test:down"
  },
  "author": "Udacity",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2",
    "dotenv": "^17.4.2",
    "express": "^4.17.1",
    "jsonwebtoken": "^9.0.3",
    "jwt": "^0.2.0",
    "pg": "^8.21.0",
    "ts-node": "^10.9.2",
    "yarn": "^1.22.22"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/connect": "^3.4.38",
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^8.2.3",
    "@types/express": "^5.0.6",
    "@types/jasmine": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^25.9.3",
    "@types/pg": "^8.20.0",
    "@types/supertest": "^7.2.0",
    "cross-env": "^10.1.0",
    "jasmine": "^6.3.0",
    "jasmine-spec-reporter": "^7.0.0",
    "supertest": "^7.2.2",
    "tsc-watch": "^4.2.9",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
