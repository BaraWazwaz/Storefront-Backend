# Setup

clone this repository, then create a proper `.env` file guided by the `.env.example` file provided, then run the following commands in a linux based terminal (I used Git Bash) :
```sh
yarn
docker compose up -d
npm run db:init
```